
import { favorited } from 'message-board/helpers/favorited';
import { module, test } from 'qunit';

module('Unit | Helper | favorited');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = favorited([42]);
  assert.ok(result);
});

