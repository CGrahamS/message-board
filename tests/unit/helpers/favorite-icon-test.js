
import { favoriteIcon } from 'message-board/helpers/favorite-icon';
import { module, test } from 'qunit';

module('Unit | Helper | favorite icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = favoriteIcon([42]);
  assert.ok(result);
});

